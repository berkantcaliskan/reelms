/**
 * Dashboard feature boundary.
 *
 * Keep new dashboard UI, hooks, services, and tests here. Existing behavior still
 * lives in features/legacy/LegacyReelmsApp.jsx until this feature is extracted.
 */
export const dashboardFeatureStatus = 'pending-extraction'
