/**
 * Friends feature boundary.
 *
 * Keep new friends UI, hooks, services, and tests here. Existing behavior still
 * lives in features/legacy/LegacyReelmsApp.jsx until this feature is extracted.
 */
export const friendsFeatureStatus = 'pending-extraction'
