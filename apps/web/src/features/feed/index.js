/**
 * Feed feature boundary.
 *
 * Keep new feed UI, hooks, services, and tests here. Existing behavior still
 * lives in features/legacy/LegacyReelmsApp.jsx until this feature is extracted.
 */
export const feedFeatureStatus = 'pending-extraction'
