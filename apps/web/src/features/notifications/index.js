/**
 * Notifications feature boundary.
 *
 * Keep new notifications UI, hooks, services, and tests here. Existing behavior still
 * lives in features/legacy/LegacyReelmsApp.jsx until this feature is extracted.
 */
export const notificationsFeatureStatus = 'pending-extraction'
