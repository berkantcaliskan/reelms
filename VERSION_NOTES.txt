
NOTES

Dinamik bar ikonları yuvarlatılmış görünüme getir.
/ Direkt mesajlar dinamik barda görünmez oluyor.
/ Ses kanalından içeriden çıkma eylemi çalışmıyor. Panelden çıkış çalışıyor.

Reelm görseli büyük. Küçültülebilir.

/ Mesajlar görüntüleme hatası verebiliyor.
/ Bağlam çalıştırmada sorun oluşabiliyor.
/ Mesaja ifade bırakma işleminde emoji penceresi, ikon üzerinden çıkınca kapanıyor.
/ Spotify connect çalışmıyor.

--------------------------------------------------------------------------------

TO DO

[ ] Prodüksiyon .env doğrulaması
  - EC2 sunucusundaki .env dosyasının doğru değerlere sahip olduğu kontrol edilmeli.
  - NODE_ENV=production, GOOGLE_REDIRECT_URI, PUBLIC_API_URL, PUBLIC_WEB_URL

[ ] Deploy pipeline kurulumu
  - Şu an deploy manuel yapılıyor (git pull + build + restart).
  - GitHub Actions ile otomatik deploy eklenebilir.

[ ] E-posta doğrulama akışı iyileştirmesi
  - Kullanıcı doğrulanmamış e-posta ile giriş yapmaya çalışırsa daha net mesaj.
  - "Verify your e-mail" mesajı şu an backend'den geliyor, özel UI eklenebilir.

[ ] E-posta gönderimi (Resend) doğrulanması
  - admin@reelms.io domain'i Resend'de verified olmalı.
  - Doğrulama e-postasının gerçekten gidip gitmediği test edilmeli.

[ ] SSH / deploy erişimi netleştirilmesi
  - EC2 security group port 22 kapalı veya kısıtlı, deploy süreci netleştirilmeli.
  - Key dosyası konumu ve kullanıcı adı (ubuntu / ec2-user) netleştirilmeli.

[ ] E-posta ile giriş testi
  - Kayıt + e-posta doğrulama + giriş tam akışı test edilmeli.

--------------------------------------------------------------------------------
NOTLAR
--------------------------------------------------------------------------------

- Supabase key formatı: sb_secret_... (yeni format, JWT değil)
- Storage driver: supabase
- E-posta sağlayıcı: resend
- API domain: api.reelms.io
- Web domain: reelms.io
- EC2 key: relmo.pem

DEĞİŞİKLİKLER

Dinamik barda güncellemeler yapıldı.
Yeni tema Gece eklendi.
Tema önizlemeleri güncellendi.
Mesajların daha hızlı yüklenmesi için: transports: ['polling', 'websocket'] < değiştirildi > transports: ['websocket', 'polling']
Temalar güncellendi.
"Yazıyor" ibaresi eklendi.
Yanıtla özelliği eklendi.
